# Simulated test run — POC-9 ARM Template Fixer Loop (fix verb)

> ⚠️ Simulated outcome. Live validation in VS Code with ARM MCP server attached.
> Generated by builder-poc-9 on 2026-05-12.
> run_id: FIX-20260512-<simulated>broken-vnet.json</simulated>-<simulated>a3f9c1d7</simulated>

---

## Input Template (broken-vnet.json)

```json
{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "resources": [
    {
      "type": "Microsoft.Network/networkInterfaces",
      "apiVersion": "2023-04-01",
      "name": "myNic",
      "location": "[resourceGroup().location]",
      "properties": {
        "ipConfigurations": [
          {
            "name": "ipconfig1",
            "properties": {
              "subnet": {
                "id": "[resourceId('Microsoft.Network/virtualNetworks/subnets', 'myVnet', 'default')]"
              }
            }
          }
        ]
      }
    },
    {
      "type": "Microsoft.Network/virtualNetworks",
      "apiVersion": "2023-04-01",
      "name": "myVnet",
      "location": "[resourceGroup().location]",
      "properties": {
        "addressSpace": { "addressPrefixes": ["10.0.0.0/16"] },
        "subnets": [{ "name": "default", "properties": { "addressPrefix": "10.0.0.0/24" } }]
      },
      "sku": { "name": "UltraFast_v99" }
    }
  ]
}
```

**Loaded runbook:** `runbooks/prod.yaml`  
**Subscription:** <simulated><SUBSCRIPTION_ID></simulated>  
**Resource group:** <simulated>arm-poc-rg</simulated>  
**max_retries:** 3  
**ruleset_hash8:** <simulated>a3f9c1d7</simulated>

---

## Attempt 1 of 3

### Step 2 — Initial deploy attempt (simulated)

```
[Attempt 1/3] Simulating deployment of broken-vnet.json to arm-poc-rg
              in subscription <SUBSCRIPTION_ID>

Would call: create_template_deployment(
  subscriptionId: "<simulated><SUBSCRIPTION_ID></simulated>",
  resourceGroupName: "<simulated>arm-poc-rg</simulated>",
  deploymentName: "fix-20260512-attempt-1",
  mode: "Incremental",
  template: <contents of broken-vnet.json>
)
[NOT EXECUTED in v1 — simulated error response below]
```

**Fabricated ARM error response:**

<simulated>
```json
{
  "status": "Failed",
  "error": {
    "code": "MissingDependency",
    "message": "Resource 'myNic' depends on resource 'myVnet' which has not been provisioned. Ensure 'myVnet' is deployed before 'myNic'.",
    "details": [
      {
        "code": "ResourceNotFound",
        "target": "Microsoft.Network/virtualNetworks/myVnet",
        "message": "Resource not yet available."
      }
    ]
  }
}
```
</simulated>

### Step 3 — Error classification

| Field | Value |
|---|---|
| Extracted code | `MissingDependency` |
| Matched rule | R001 |
| Fix pattern | `add dependsOn` |

### Step 4 — Apply fix pattern (R001 — add dependsOn)

Action: Add `dependsOn: ["[resourceId('Microsoft.Network/virtualNetworks', 'myVnet')]"]`
to the `myNic` resource.

### Step 5 — Diff log (Attempt 1 → 2)

```diff
--- template (attempt 1)
+++ template (attempt 2)
@@ -6,6 +6,11 @@
       "name": "myNic",
       "location": "[resourceGroup().location]",
+      "dependsOn": [
+        "[resourceId('Microsoft.Network/virtualNetworks', 'myVnet')]"
+      ],
       "properties": {
```

### Step 6 — Halt-on-destructive-change check

Scanned current diff against `destructive_change_types`. No `Microsoft.KeyVault/vaults`,
`Microsoft.Sql/servers`, `Microsoft.Storage/storageAccounts`, `Microsoft.Network/virtualNetworks`
deletions detected. (VNet is being **added**, not deleted.) ✅ Safe to continue.

---

## Attempt 2 of 3

### Step 2 — Simulated redeploy

```
[Attempt 2/3] Simulating deployment of broken-vnet.json (post-attempt-1 fix) to arm-poc-rg

Would call: create_template_deployment(
  deploymentName: "fix-20260512-attempt-2",
  mode: "Incremental",
  template: <contents of template after attempt-1 fix>
)
[NOT EXECUTED in v1 — simulated error response below]
```

**Fabricated ARM error response:**

<simulated>
```json
{
  "status": "Failed",
  "error": {
    "code": "InvalidSku",
    "message": "The SKU 'UltraFast_v99' is not available for resource type 'Microsoft.Network/virtualNetworks' in location 'eastus'. Please select a valid SKU.",
    "details": []
  }
}
```
</simulated>

### Step 3 — Error classification

| Field | Value |
|---|---|
| Extracted code | `InvalidSku` |
| Matched rule | R002 |
| Fix pattern | `suggest valid SKU` |

### Step 4 — Apply fix pattern (R002 — suggest valid SKU)

Action: Remove the invalid `sku` block from `myVnet` (VNets do not use a `sku.name` field in
this form; the property was spurious). Substituting with no SKU block (standard VNet).

### Step 5 — Diff log (Attempt 2 → 3)

```diff
--- template (attempt 2)
+++ template (attempt 3)
@@ -28,7 +28,6 @@
         "subnets": [{ "name": "default", "properties": { "addressPrefix": "10.0.0.0/24" } }]
-      },
-      "sku": { "name": "UltraFast_v99" }
+      }
     }
```

### Step 6 — Halt-on-destructive-change check

Scanned diff. Only a `sku` property block removed — no resource deletion or replacement.
`Microsoft.Network/virtualNetworks` resource itself is not being deleted. ✅ Safe to continue.

---

## Attempt 3 of 3

### Step 2 — Simulated redeploy

```
[Attempt 3/3] Simulating deployment of broken-vnet.json (post-attempt-2 fix) to arm-poc-rg

Would call: create_template_deployment(
  deploymentName: "fix-20260512-attempt-3",
  mode: "Incremental",
  template: <contents of template after attempt-2 fix>
)
[NOT EXECUTED in v1 — simulated success response below]
```

**Fabricated ARM success response:**

<simulated>
```json
{
  "status": "Succeeded",
  "properties": {
    "provisioningState": "Succeeded",
    "deploymentName": "fix-20260512-attempt-3",
    "timestamp": "<simulated>2026-05-12T06:42:17Z</simulated>",
    "duration": "PT18.4S"
  }
}
```
</simulated>

**✅ Deployment succeeded on attempt 3.**

---

## Final Summary

Rendered from `templates/output-report.md`:

---

# ARM Template Fix Report

**Run ID:** FIX-20260512-<simulated>broken-vnet.json</simulated>-<simulated>a3f9c1d7</simulated>  
**Template:** <simulated>broken-vnet.json</simulated>  
**Generated (UTC):** <simulated>2026-05-12T06:42:19Z</simulated>  
**Ruleset hash:** <simulated>a3f9c1d7</simulated>

## Summary

| Metric | Value |
|---|---|
| Final status | <simulated>SUCCESS</simulated> |
| Attempts used | <simulated>3</simulated> |
| Max retries | 3 |
| Last error code | <simulated>InvalidSku</simulated> |
| Fixed template | `exports/FIX-20260512-broken-vnet.json-a3f9c1d7-fixed.json` |

## Attempt Changelog

| Attempt | Error Code | Rule Matched | Fix Applied | Outcome |
|---:|---|---|---|---|
| 1 | <simulated>MissingDependency</simulated> | R001 | add dependsOn to myNic → myVnet | <simulated>Failed</simulated> (next attempt) |
| 2 | <simulated>InvalidSku</simulated> | R002 | removed spurious sku block from myVnet | <simulated>Failed</simulated> (next attempt) |
| 3 | — | — | No fix needed | **<simulated>Succeeded</simulated>** |

## Diff Log

**Attempt 1 → 2:**
```diff
--- template (attempt 1)
+++ template (attempt 2)
@@ -6,6 +6,11 @@
       "name": "myNic",
       "location": "[resourceGroup().location]",
+      "dependsOn": [
+        "[resourceId('Microsoft.Network/virtualNetworks', 'myVnet')]"
+      ],
       "properties": {
```

**Attempt 2 → 3:**
```diff
--- template (attempt 2)
+++ template (attempt 3)
@@ -28,7 +28,6 @@
         "subnets": [{ "name": "default", "properties": { "addressPrefix": "10.0.0.0/24" } }]
-      },
-      "sku": { "name": "UltraFast_v99" }
+      }
     }
```

## Fixed Template

The corrected template has been written to
`exports/FIX-20260512-broken-vnet.json-a3f9c1d7-fixed.json`.

---
*Run `@arm-template-fixer status` to check a deployment in progress. Run `@arm-template-fixer cancel` to cancel a hanging deployment (requires confirmation).*

---

✅ CSV append confirmed → `exports/fix-run-log.csv` row appended:
`FIX-20260512-broken-vnet.json-a3f9c1d7,2026-05-12T06:42:19Z,broken-vnet.json,a3f9c1d7,3,SUCCESS,InvalidSku`

---

## Halt-on-Destructive-Change Scenario (separate block)

> This block demonstrates what happens if a fix attempt would delete a protected resource type.

**Hypothetical input:** A fix for a `Conflict` error (R004) proposes switching to `Complete` mode,
which would drop `Microsoft.KeyVault/vaults/myKeyVault` (present in the live RG but absent from
the template).

**Simulated error at attempt 2:**

<simulated>
```json
{
  "status": "Failed",
  "error": {
    "code": "Conflict",
    "message": "Resource 'myStorageAccount' already exists."
  }
}
```
</simulated>

**Fix proposed (R004):** Switch deployment mode from `Incremental` to `Complete`.

**Step 6 — Halt-on-destructive-change check:**

```
Scanning proposed diff for destructive_change_types...
  Deployment mode change: Incremental → Complete
  Resources in live RG but NOT in template:
    - Microsoft.KeyVault/vaults/myKeyVault  ← MATCH in destructive_change_types
HALT triggered.
```

**Output (rendered from templates/output-status.md):**

```
# ARM Template Fix Status

Run ID:   FIX-20260512-broken-storage.json-a3f9c1d7
Template: broken-storage.json
Generated (UTC): <simulated>2026-05-12T06:43:01Z</simulated>

STATUS=<simulated>HALTED</simulated>
REASON=<simulated>destructive-change-detected</simulated>
TYPE=Microsoft.KeyVault/vaults
ATTEMPT=2

Attempt Changelog:
  Attempt 1 | <simulated>Conflict</simulated> | R004 | proposed: switch to Complete mode | <simulated>HALTED</simulated> before apply

Next Steps:
  Remove Microsoft.KeyVault/vaults/myKeyVault from destructive_change_types if you intend
  to delete it, OR keep Incremental mode and resolve the conflict manually.
```

---

## Hypothetical Cancel Scenario

> This block demonstrates the cancel confirmation gate.

**Scenario:** A live deployment `fix-20260512-attempt-2` is running and appears hung (no status
update for 5 minutes). The user runs:

```
@arm-template-fixer cancel deployment_name=fix-20260512-attempt-2
```

**Step 7 — Cancel flow:**

1. Agent calls `get_arm_template_deployment_status(deploymentName="fix-20260512-attempt-2")`.

   Simulated response:
   <simulated>
   ```json
   { "status": "Running", "properties": { "provisioningState": "Running" } }
   ```
   </simulated>

2. Agent displays the mandatory confirmation gate:

   ```
   Deployment fix-20260512-attempt-2 is currently <simulated>Running</simulated>.
   To cancel, type exactly: confirm cancel fix-20260512-attempt-2
   Any other input will abort the cancel flow.
   ```

3. **User types:** `confirm cancel fix-20260512-attempt-2`

4. Agent calls `cancel_arm_template_deployment(deploymentName="fix-20260512-attempt-2")`.

   Simulated response:
   <simulated>
   ```json
   { "status": "Canceled" }
   ```
   </simulated>

5. Agent logs: `[Cancel] Cancellation submitted for fix-20260512-attempt-2.`

**Output:**
```
Deployment fix-20260512-attempt-2 status: <simulated>Canceled</simulated>.
Cancellation submitted successfully.
```

---

## Would Deploy (NOT EXECUTED in v1) — Appendix

Per ratification #3 (binding), `create_template_deployment` is NEVER called in v1.
The calls below are shown for operator reference only — this is what the agent *would* execute
if the `apply` verb were activated in a future v2 release.

```
# Attempt 1 (would have failed — shown for reference)
create_template_deployment(
  subscriptionId: "<SUBSCRIPTION_ID>",
  resourceGroupName: "arm-poc-rg",
  deploymentName: "fix-20260512-attempt-1",
  mode: "Incremental",
  template: { ... original broken-vnet.json ... }
)

# Attempt 2 (would have failed — shown for reference)
create_template_deployment(
  subscriptionId: "<SUBSCRIPTION_ID>",
  resourceGroupName: "arm-poc-rg",
  deploymentName: "fix-20260512-attempt-2",
  mode: "Incremental",
  template: { ... template after dependsOn fix ... }
)

# Attempt 3 (would have succeeded — shown for reference)
create_template_deployment(
  subscriptionId: "<SUBSCRIPTION_ID>",
  resourceGroupName: "arm-poc-rg",
  deploymentName: "fix-20260512-attempt-3",
  mode: "Incremental",
  template: { ... template after SKU fix ... }
)
```

Live validation is operator-driven in VS Code with the ARM MCP server attached.
