# Simulated test run — POC-4 Golden Path Provisioner (provision verb)

> ⚠️ Simulated outcome. Live validation in VS Code with ARM MCP server attached.
> Generated by builder-poc-4 on 2026-05-12.
> run_id: GP-20260512-<simulated>prod</simulated>-<simulated>a3f7c12e</simulated>

---

## 1 — User invocation

```
@golden-path-provisioner provision --workload "kubernetes api backend for platform team" --team platform --region eastus2 --scope prod
```

---

## 2 — Configuration loaded

Loaded `runbooks/prod.yaml` + `runbooks/prod.values.yaml` (operator-supplied, gitignored).

| Key | Value |
|---|---|
| `subscription_id` | <simulated>11111111-2222-3333-4444-555555555555</simulated> |
| `naming_prefix` | `gp` |
| `environment` | `prod` |
| `allowed_regions` | `eastus2`, `westus2`, `westeurope`, `northeurope` |

---

## 3 — Template selection (keyword-deterministic)

Tokenised workload: `kubernetes api backend for platform team`

Scanning `golden_path_catalog` in file order:

| Entry | Keyword checked | Match? |
|---|---|---|
| `aks-baseline` | `kubernetes` | ✅ **MATCH — first match wins** |

```
TEMPLATE SELECTED: aks-baseline (matched keyword: kubernetes)
Template path: skills/golden-path-provisioner/remediation/golden-path-aks-baseline.json
```

---

## 4 — Naming + Tag Plan

Region `eastus2` ✅ in `allowed_regions`.

### Resource Names

| Resource Type | Generated Name |
|---|---|
| AKS cluster (`managedclusters`) | `gp-platform-eastus2-aks` |
| Container Registry (`registries`) | `gpplatformeastus2acr` |
| Key Vault (`vaults`) | `gp-platform-eastus2-kv` |
| Log Analytics workspace (`workspaces`) | `gp-platform-eastus2-la` |

### Tags

| Key | Value |
|---|---|
| `Team` | `platform` |
| `Environment` | `prod` |
| `ManagedBy` | `golden-path-provisioner` |
| `CostCenter` | <simulated>CC-9900</simulated> |
| `DeployedOn` | `20260512` |

---

## 5 — Pre-Deploy Compliance Check

Called `execute_query` (directly — no `validate_query` per tool allowlist) for each rule:

| Rule ID | Title | Status | Failing Resources |
|---|---|---|---:|
| R001 | Resources not following golden-path naming convention | <simulated>PASS</simulated> | <simulated>0</simulated> |
| R002 | Golden-path resources missing required tags | <simulated>PASS</simulated> | <simulated>0</simulated> |
| R003 | Public IP addresses exposed on golden-path resources | <simulated>PASS</simulated> | <simulated>0</simulated> |

No pre-deploy findings. Proceeding to what-if plan.

---

## 6 — What-If Plan Summary

**Selected template:** `aks-baseline` (`golden-path-aks-baseline.json`)
**Estimated resources:** 4

| Resource | Name | Type |
|---|---|---|
| Log Analytics Workspace | `gp-platform-eastus2-la` | `Microsoft.OperationalInsights/workspaces` |
| Key Vault | `gp-platform-eastus2-kv` | `Microsoft.KeyVault/vaults` |
| Container Registry | `gpplatformeastus2acr` | `Microsoft.ContainerRegistry/registries` |
| AKS Cluster | `gp-platform-eastus2-aks` | `Microsoft.ContainerService/managedClusters` |

**Parameters that would be passed:**

| Parameter | Value |
|---|---|
| `namingPrefix` | `gp` |
| `team` | `platform` |
| `location` | `eastus2` |
| `environment` | `prod` |
| `aksNodeCount` | `3` |
| `aksNodeVmSize` | `Standard_D4s_v5` |
| `logAnalyticsRetentionDays` | `30` |

---

## 7 — Progress Polling (simulated — live continuation of provision verb)

*These lines simulate what `get_arm_template_deployment_status` would return during a live run.*
*Polling interval: 10 seconds fixed.*

**T+10s:**
<simulated>
```
Deployment : gp-platform-eastus2-20260512-abc1
Resource Group: gp-platform-eastus2-rg
Provisioning State: Running
Resources:
  gp-platform-eastus2-la   → Succeeded
  gp-platform-eastus2-kv   → Running
  gpplatformeastus2acr      → NotStarted
  gp-platform-eastus2-aks   → NotStarted
```
</simulated>

**T+20s:**
<simulated>
```
Deployment : gp-platform-eastus2-20260512-abc1
Provisioning State: Running
Resources:
  gp-platform-eastus2-la   → Succeeded
  gp-platform-eastus2-kv   → Succeeded
  gpplatformeastus2acr      → Running
  gp-platform-eastus2-aks   → NotStarted
```
</simulated>

**T+30s (hypothetical failure — triggers cancel flow):**
<simulated>
```
Deployment : gp-platform-eastus2-20260512-abc1
Provisioning State: Failed
Resources:
  gp-platform-eastus2-la   → Succeeded
  gp-platform-eastus2-kv   → Succeeded
  gpplatformeastus2acr      → Failed  ← sub-resource failure
  gp-platform-eastus2-aks   → Canceled

⚠ DEPLOYMENT FAILED — run cancel verb to trigger rollback hook
```
</simulated>

---

## 8 — Cancel Flow (simulated — triggered by sub-resource failure at T+30s)

The agent automatically presents the cancel confirmation gate:

```
⚠ CANCEL REQUEST
Deployment : gp-platform-eastus2-<simulated>20260512-abc1</simulated>
Resource Group: <simulated>gp-platform-eastus2-rg</simulated>
Current State : <simulated>Failed</simulated>

Type "confirm" to proceed with cancellation. Any other input aborts.
```

**User types:** `confirm`

**Agent calls:** `cancel_arm_template_deployment`
- Deployment: <simulated>gp-platform-eastus2-20260512-abc1</simulated>
- Resource group: <simulated>gp-platform-eastus2-rg</simulated>
- Outcome: <simulated>Canceled</simulated>

**Audit log line written to `exports/cancel-audit.jsonl`:**
```json
{"event":"cancel_attempt","deployment_name":"<simulated>gp-platform-eastus2-20260512-abc1</simulated>","resource_group":"<simulated>gp-platform-eastus2-rg</simulated>","confirmed":true,"outcome":"<simulated>Canceled</simulated>","utc":"<simulated>2026-05-12T06:50:30Z</simulated>"}
```

```
CANCEL SUBMITTED — monitor with status verb
```

---

## Appendix — Would deploy (NOT EXECUTED in v1)

```
WOULD DEPLOY (NOT EXECUTED in v1 — what-if only per locked decision)
```

- `create_template_deployment` was **not called** during this test run.
- This is the expected behaviour for v1 per ratification #3 (locked decision):
  *"Templates only — never call `create_template_deployment` in any test path."*
- The cancel flow above simulates the path the agent would take during a live failing deployment.
  In v1, the cancel call is also simulated (not executed).
- To run this PoC live, attach the ARM MCP server in VS Code, supply a real `prod.values.yaml`,
  and use the `apply` verb (future version) or manually invoke `create_template_deployment`.
