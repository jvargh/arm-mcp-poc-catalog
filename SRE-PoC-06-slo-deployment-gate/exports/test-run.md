# Simulated test run — SRE-PoC-6 SLO-Aware Deployment Gate (gate verb)

> ⚠️ Simulated outcome. Live validation in VS Code with ARM MCP server attached.
> Generated by builder-sre-poc-6 on 2026-05-12.
> run_id: SLO-20260512-<simulated>prod</simulated>-<simulated>a3f8c1d2</simulated>

---

## Scenario A — Service tag resolves, budget 72.4% (above 50% threshold) → ALLOW + would-deploy notice

### Input

```
@slo-deployment-gate deploy scope prod service payments-api template infra/payments-api.json
```

Runbook value: `simulated_budget_pct: 72.4`, `budget_threshold_pct: 50.0`

### Step 1 — Load configuration

```
scope:        prod
subscription: <simulated>11111111-2222-3333-4444-555555555555</simulated>
service:      payments-api
run_id:       SLO-20260512-prod-a3f8c1d2
ruleset_hash8: a3f8c1d2
```

### Step 2 — Rule R001 (generate_query → execute_query)

**generate_query call:**
```json
{
  "tool": "generate_query",
  "query_text": "Resources | where type in~ ('microsoft.insights/components','microsoft.operationalinsights/workspaces') | where tags['slo_service'] =~ 'payments-api' | project id, name, resourceGroup, subscriptionId, location, workspace_id = tostring(properties.customerId), service_tag = tostring(tags['slo_service'])",
  "subscriptions": ["<simulated>11111111-2222-3333-4444-555555555555</simulated>"]
}
```

**execute_query result:**
```json
[
  {
    "id": "<simulated>/subscriptions/11111111-2222-3333-4444-555555555555/resourceGroups/payments-prod-rg/providers/microsoft.insights/components/payments-api-insights</simulated>",
    "name": "<simulated>payments-api-insights</simulated>",
    "resourceGroup": "<simulated>payments-prod-rg</simulated>",
    "subscriptionId": "<simulated>11111111-2222-3333-4444-555555555555</simulated>",
    "location": "<simulated>eastus2</simulated>",
    "workspace_id": "<simulated>aaaabbbb-cccc-dddd-eeee-ffffgggghhh1</simulated>",
    "service_tag": "payments-api"
  }
]
```

R001 status: **PASS** — workspace resolved.
`workspace_resource_id = /subscriptions/.../payments-api-insights` <simulated>

### Step 2 — Rule R002 (generate_query → execute_query)

**execute_query result:**
```json
[
  {
    "id": "<simulated>/subscriptions/11111111-2222-3333-4444-555555555555/resourceGroups/payments-prod-rg/providers/microsoft.insights/components/payments-api-insights</simulated>",
    "service_tag": "payments-api",
    "budget_tag_pct": "",
    "workspace_resource_id": "<simulated>/subscriptions/11111111-2222-3333-4444-555555555555/resourceGroups/payments-prod-rg/providers/microsoft.insights/components/payments-api-insights</simulated>"
  }
]
```

`budget_tag_pct` is empty → fallback to runbook `simulated_budget_pct`.
Emitting: `STATUS=OUT_OF_SCOPE REASON=arg-cannot-read-metrics WORKSPACE=<simulated>...payments-api-insights</simulated>`
`budget_pct = 72.4%` (from runbook)

R002 status: **PASS (simulated)**

### Step 2 — Rule R003 (generate_query → execute_query)

**execute_query result:**
```json
[]
```

No active incidents. R003 status: **PASS** (empty result = no incidents).

### Step 3 — Gate Decision

```
budget_pct        = 72.4%
budget_threshold  = 50.0%
72.4 > 50.0       → DECISION = ALLOW
```

### Rendered output-gate-result.md

---

# SLO Deployment Gate Result

**Run ID:** SLO-20260512-<simulated>prod</simulated>-<simulated>a3f8c1d2</simulated>
**Scope:** prod
**Service:** payments-api
**Generated (UTC):** <simulated>2026-05-12T06:00:00Z</simulated>
**Ruleset hash:** <simulated>a3f8c1d2</simulated>

## SLO Budget Evaluation

| Field | Value |
|---|---|
| Service | payments-api |
| SLO Error Budget | 72.4% |
| Budget Threshold | 50.0% |
| Window | 30 days |
| Budget Source | runbook `slo_targets` (v1 simulated) |
| Workspace Resource ID | <simulated>/subscriptions/11111111-2222-3333-4444-555555555555/resourceGroups/payments-prod-rg/providers/microsoft.insights/components/payments-api-insights</simulated> |

> ℹ️ `STATUS=OUT_OF_SCOPE REASON=arg-cannot-read-metrics WORKSPACE=<simulated>/subscriptions/.../payments-api-insights</simulated>`
> ARG located the App Insights workspace but cannot read live metric values.
> Investigate the workspace directly for live SLO data.

## Rule Evaluation

| Rule ID | Title | Status | Result |
|---|---|---|---|
| R001 | Resolve service → App Insights component by tag | PASS | Workspace resolved |
| R002 | Check error budget status via workspace metadata | PASS (simulated) | budget_pct=72.4% (runbook) |
| R003 | Validate no active incidents on target service | PASS | No active incidents |

## Decision

**Decision:** ALLOW

---

### Would-Deploy Notice (deploy mode)

> ⚠️ **v1 — What-if only. No actual deployment was issued.**

```
create_template_deployment(
  template_path = "infra/payments-api.json",
  scope         = "prod",
  service       = "payments-api",
  mode          = "Incremental"
)
```

Gate result archived at `exports/gate-SLO-20260512-prod-a3f8c1d2.md`.

---

## Scenario B — Service budget 18.2% (below 50% threshold) → BLOCK + bypass demo

### Input

```
@slo-deployment-gate deploy scope prod service checkout-api template infra/checkout-api.json
```

Runbook value: `simulated_budget_pct: 18.2`, `budget_threshold_pct: 50.0`

### Step 2 — Rule R001

**execute_query result:**
```json
[
  {
    "id": "<simulated>/subscriptions/11111111-2222-3333-4444-555555555555/resourceGroups/checkout-prod-rg/providers/microsoft.insights/components/checkout-api-insights</simulated>",
    "name": "<simulated>checkout-api-insights</simulated>",
    "resourceGroup": "<simulated>checkout-prod-rg</simulated>",
    "subscriptionId": "<simulated>11111111-2222-3333-4444-555555555555</simulated>",
    "service_tag": "checkout-api"
  }
]
```

R001 status: **PASS** — workspace resolved.

### Step 2 — Rule R002

`budget_tag_pct` is empty → fallback to runbook `simulated_budget_pct = 18.2`.
Emitting: `STATUS=OUT_OF_SCOPE REASON=arg-cannot-read-metrics WORKSPACE=<simulated>...checkout-api-insights</simulated>`
`budget_pct = 18.2%`

### Step 2 — Rule R003

Empty result → no active incidents. R003: **PASS**.

### Step 3 — Gate Decision

```
budget_pct        = 18.2%
budget_threshold  = 50.0%
18.2 ≤ 50.0       → DECISION = BLOCK
```

### Rendered output-gate-result.md

---

# SLO Deployment Gate Result

**Run ID:** SLO-20260512-<simulated>prod</simulated>-<simulated>a3f8c1d2</simulated>
**Scope:** prod
**Service:** checkout-api
**Generated (UTC):** <simulated>2026-05-12T06:07:00Z</simulated>
**Ruleset hash:** <simulated>a3f8c1d2</simulated>

## SLO Budget Evaluation

| Field | Value |
|---|---|
| Service | checkout-api |
| SLO Error Budget | 18.2% |
| Budget Threshold | 50.0% |
| Window | 30 days |
| Budget Source | runbook `slo_targets` (v1 simulated) |
| Workspace Resource ID | <simulated>/subscriptions/11111111-2222-3333-4444-555555555555/resourceGroups/checkout-prod-rg/providers/microsoft.insights/components/checkout-api-insights</simulated> |

> ℹ️ `STATUS=OUT_OF_SCOPE REASON=arg-cannot-read-metrics WORKSPACE=<simulated>/subscriptions/.../checkout-api-insights</simulated>`
> ARG located the App Insights workspace but cannot read live metric values.
> Investigate the workspace directly for live SLO data.

## Rule Evaluation

| Rule ID | Title | Status | Result |
|---|---|---|---|
| R001 | Resolve service → App Insights component by tag | PASS | Workspace resolved |
| R002 | Check error budget status via workspace metadata | PASS (simulated) | budget_pct=18.2% (runbook) |
| R003 | Validate no active incidents on target service | PASS | No active incidents |

## Decision

**Decision:** BLOCK

> 🚫 Deployment blocked. SLO error budget (18.2%) is at or below the threshold (50.0%).
> To bypass, resubmit with a valid CR or incident reference:
> `@slo-deployment-gate deploy ... bypass CR-XXXXXX`

---

### Bypass Demo

User resubmits:
```
@slo-deployment-gate deploy scope prod service checkout-api template infra/checkout-api.json bypass CR-001234
```

Agent validates bypass reference: `CR-001234` matches pattern `^(CR|INC)-[0-9]{6}$` → **valid**.

Decision overridden: `ALLOW (BYPASS)`

**Audit log line written to `exports/bypass-audit.jsonl`:**
```json
{"timestamp_utc":"<simulated>2026-05-12T06:09:15Z</simulated>","service":"checkout-api","budget_pct":18.2,"cr_or_incident_ref":"CR-001234","approver":"<simulated>operator@contoso.com</simulated>","run_id":"SLO-20260512-prod-a3f8c1d2"}
```

**Rendered output-gate-result.md (ALLOW BYPASS):**

---

# SLO Deployment Gate Result

**Run ID:** SLO-20260512-<simulated>prod</simulated>-<simulated>a3f8c1d2</simulated>
**Scope:** prod
**Service:** checkout-api
**Generated (UTC):** <simulated>2026-05-12T06:09:15Z</simulated>
**Ruleset hash:** <simulated>a3f8c1d2</simulated>

## SLO Budget Evaluation

| Field | Value |
|---|---|
| Service | checkout-api |
| SLO Error Budget | 18.2% |
| Budget Threshold | 50.0% |
| Window | 30 days |
| Budget Source | runbook `slo_targets` (v1 simulated) |
| Workspace Resource ID | <simulated>/subscriptions/.../checkout-api-insights</simulated> |

## Rule Evaluation

| Rule ID | Title | Status | Result |
|---|---|---|---|
| R001 | Resolve service → App Insights component by tag | PASS | Workspace resolved |
| R002 | Check error budget status via workspace metadata | PASS (simulated) | budget_pct=18.2% (runbook) |
| R003 | Validate no active incidents on target service | PASS | No active incidents |

## Decision

**Decision:** ALLOW (BYPASS)

> ⚠️ **Bypass granted.** CR reference `CR-001234` accepted. Budget was below threshold (18.2% ≤ 50.0%).
> Bypass audit event written to `exports/bypass-audit.jsonl`.

---

**Would-Deploy Notice (deploy mode — ALLOW BYPASS path):**

> ⚠️ **v1 — What-if only. No actual deployment was issued.**

```
create_template_deployment(
  template_path = "infra/checkout-api.json",
  scope         = "prod",
  service       = "checkout-api",
  mode          = "Incremental"
)
```

Gate result archived at `exports/gate-SLO-20260512-prod-a3f8c1d2.md`.

---

*End of simulated test run. Live validation requires VS Code with ARM MCP server attached and a signed-in Azure identity with Reader + Resource Graph Reader.*
