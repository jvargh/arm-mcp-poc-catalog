{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_description": "Sample ARM template referenced by README examples for SRE-PoC-6 SLO Deployment Gate. v1 of the gate never calls create_template_deployment; this file exists only so the 'template infra/main.json' argument resolves to a real, syntactically-valid template path. Replace with your real workload template before wiring up live deploys."
  },
  "parameters": {
    "serviceName": {
      "type": "string",
      "defaultValue": "sample-service",
      "metadata": {
        "description": "Name of the service this deployment targets. Must match an entry in runbooks/<scope>.yaml slo_targets and the slo_service tag on the App Insights component."
      }
    },
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]",
      "metadata": {
        "description": "Azure region for the deployment."
      }
    }
  },
  "variables": {},
  "resources": [],
  "outputs": {
    "serviceName": {
      "type": "string",
      "value": "[parameters('serviceName')]"
    },
    "location": {
      "type": "string",
      "value": "[parameters('location')]"
    },
    "noOp": {
      "type": "string",
      "value": "This is a no-op placeholder template. Replace resources[] with your real workload before live deploys."
    }
  }
}
