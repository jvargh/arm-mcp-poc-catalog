# Bishop — History

## Project Context

- **Project:** arm-mcp — ARM MCP PoC catalog
- **Stack:** Markdown + YAML + JSON ARM templates. Each PoC is a self-contained VS Code workspace folder with its own `.vscode/mcp.json` declaring the Azure Resource Manager MCP Server (`https://mcp.management.azure.com`).
- **Reference implementation:** `reliability-posture-scorecard/` — copy the layout verbatim. 19 files: README, agent.md, SKILL.md, rules.yaml, 3 templates, 3 remediation ARM stubs, 3 prompts, runbook, mcp.json, exports/.
- **Source-of-truth specs:** `ARM-MCP-PoC-Ideas.md` (PoC 1–12, enterprise) + `ARM-MCP-SRE-PoCs.md` (SRE-PoC 1–12). Skip SRE-PoC 5 (already implemented).
- **User:** Copilot Scribe (per `git config user.name`). Working from `C:\Users\varghesejoji\Desktop\squad-test\arm-mcp`.
- **Test scope:** Subscription `463a82d4-1896-4332-aeeb-618ee5a5aa93`; RGs `aks01day2-rg`, `aks02day2-rg`, `aksnapday2-rg`, `apicenter-rg`, `apptesting-rg`, `az-foundry-rg`.

## Locked Decisions (from session start)

1. **Parallelism cap:** 6 sub-agents per wave, 4 waves total (6+6+6+5).
2. **Validation depth:** schema + parity only. No live ARM MCP execution from sub-agents (tools not in their surface). User runs live validation manually in VS Code.
3. **Deploy PoCs (POC-4, POC-9, SRE-PoC-7, SRE-PoC-10):** what-if templates only — `create_template_deployment` is NEVER called in any test path.
4. **Commit strategy:** single commit at the end with all 23 folders + `FLEET-BUILD-REPORT.md`.
5. **Report venue:** `FLEET-BUILD-REPORT.md` at repo root (no GitHub PR/issue).
6. **Skip:** SRE-PoC 5 (Reliability posture scorecard) — already exists at `reliability-posture-scorecard/`.

## Folder Naming Convention

- Enterprise PoCs: `POC-{n}-{kebab-title}/` (POC-1 through POC-12)
- SRE PoCs: `SRE-PoC-{n}-{kebab-title}/` (SRE-PoC-1 through SRE-PoC-12, skipping 5)
- All folders at repo root, peers of `reliability-posture-scorecard/`.

## Determinism Contract (paste into every builder prompt)

1. KQL is read from `rules/*.yaml`, never generated by the LLM. String substitution for scope filters only.
2. Every ARG call sequence: `validate_query` → `execute_query`. On `validate_query` failure: mark `INVALID`, do not retry, continue.
3. `run_id` format: `<POC-PREFIX>-{YYYYMMDD}-{scope}-{sha256(rules.yaml)[:8]}`.
4. Sort orders, column headers, section order are pinned in `SKILL.md` and templates.
5. Numeric output is integer unless source data is a percentage (1 decimal).
6. Deploy-capable PoCs: never deploy without explicit user verb (e.g. `apply`, `remediate`) AND a confirm gate; honor global freeze flag from `runbooks/prod.yaml`.
7. PoCs with cancel paths must implement and test (schema/parity test) the cancel path.
8. Tool allowlist in `agents/<slug>.agent.md` MUST match exactly the PoC's "Tools used" line in the source MD — no additions, no omissions.

## Learnings

### 2026-05-12 — Fleet Manifest Produced

- **Manifest path:** `.squad/decisions/inbox/bishop-fleet-manifest.md`
- **Wave plan:** Wave 1 (POC-3,5,7,8,10,12 — pure read-only), Wave 2 (POC-6, SRE-1,9,11, POC-1,11 — cancel + simple deploy), Wave 3 (POC-2, SRE-2,3,6,7,8 — deploy gates), Wave 4 (POC-4,9, SRE-4,10,12 — complex multi-verb)
- **NEEDS_USER_DECISION rows:** POC-2 (what-if mode), POC-3 (resourcechanges table), POC-7 (policy alias KQL exception), POC-10 (authorizationresources joins), SRE-PoC-3 (quota/capacity via ARG), SRE-PoC-6 (SLO budget not in ARG), SRE-PoC-7 (quota limits not in ARG)
- **Classification:** 10 READ-ONLY, 13 DEPLOY
- **Patterns observed across PoCs:**
  - 8 of 23 PoCs need the `resourcechanges` table (POC-3, POC-5, POC-6, SRE-PoC-1, SRE-PoC-3, SRE-PoC-9, SRE-PoC-11, SRE-PoC-12) — builders should share KQL patterns.
  - 5 PoCs use `authorizationresources` (POC-10, SRE-PoC-1, SRE-PoC-4, SRE-PoC-11, SRE-PoC-8) — confirm table accessibility early.
  - 12 of 23 PoCs lack `validate_query` in their tool allowlist — they skip the validate step. Builders must document this deviation.
  - POC-6 and SRE-PoC-1 are near-identical (incident triage + cancel). SRE-PoC-8 and POC-2 share what-if blast-radius patterns.
  - POC-9 is unique: no ARG at all (ARM error codes only). Its rules.yaml schema differs from all others.
  - SRE-PoC-10 is the most complex (all 6 tools, novel runbook DSL, state machine).
