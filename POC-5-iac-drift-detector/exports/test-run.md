# Simulated test run — POC-5 IaC Drift Detector

> ⚠️ Simulated outcome. Live validation occurs in VS Code with the ARM MCP server attached.
> Generated by builder-poc-5 on 2026-05-12.
> run_id: DRIFT-20260512-prod-a3f8c1d2

---

# IaC Drift Report

**Run ID:** DRIFT-20260512-prod-a3f8c1d2
**Scope:** prod
**Generated (UTC):** 2026-05-12T05:19:47Z
**Ruleset hash:** a3f8c1d2
**Template:** `infra/main.json`

## Summary

| Metric | Value |
|---|---|
| Template resources checked | 8 |
| Resources with drift | 3 |
| Resources missing in live | 1 |
| Resources in sync | 4 |

## Drift Summary

| Resource Name | Type | Resource Group | Rule | Drifted Fields | Status |
|---|---|---|---|---|---|
| <simulated>prod-nsg-web</simulated> | microsoft.network/networksecuritygroups | <simulated>payments-prod-rg</simulated> | R001 | properties.securityRules | DRIFTED |
| <simulated>prod-vm-api01</simulated> | microsoft.compute/virtualmachines | <simulated>payments-prod-rg</simulated> | R002 | properties.hardwareProfile.vmSize | DRIFTED |
| <simulated>prodstorageacct</simulated> | microsoft.storage/storageaccounts | <simulated>checkout-prod-rg</simulated> | R003 | properties.networkAcls.defaultAction, sku.name | DRIFTED |
| <simulated>prod-kv-secrets</simulated> | microsoft.keyvault/vaults | <simulated>checkout-prod-rg</simulated> | R004 | — | MISSING_IN_LIVE |

## JSON Patch (RFC 6902)

```json
[
  {
    "resource_id": "/subscriptions/<simulated-sub-guid>/resourceGroups/payments-prod-rg/providers/Microsoft.Network/networkSecurityGroups/prod-nsg-web",
    "resource_name": "prod-nsg-web",
    "rule_id": "R001",
    "patch": [
      {
        "op": "replace",
        "path": "/properties/securityRules/0/properties/destinationPortRange",
        "value": "443"
      }
    ]
  },
  {
    "resource_id": "/subscriptions/<simulated-sub-guid>/resourceGroups/payments-prod-rg/providers/Microsoft.Compute/virtualMachines/prod-vm-api01",
    "resource_name": "prod-vm-api01",
    "rule_id": "R002",
    "patch": [
      {
        "op": "replace",
        "path": "/properties/hardwareProfile/vmSize",
        "value": "Standard_D4s_v5"
      }
    ]
  },
  {
    "resource_id": "/subscriptions/<simulated-sub-guid>/resourceGroups/checkout-prod-rg/providers/Microsoft.Storage/storageAccounts/prodstorageacct",
    "resource_name": "prodstorageacct",
    "rule_id": "R003",
    "patch": [
      {
        "op": "replace",
        "path": "/properties/networkAcls/defaultAction",
        "value": "Deny"
      },
      {
        "op": "replace",
        "path": "/sku/name",
        "value": "Standard_GRS"
      }
    ]
  }
]
```

---
*Run `@iac-drift-detector drilldown <resource_id>` for per-resource property detail.*

---

## Simulated Step Trace

The following trace shows what the agent would do step-by-step during a live run.

### Step 1 — Load configuration

- Loaded `runbooks/prod.yaml` + `runbooks/prod.values.yaml`.
- `subscriptions`: `["<simulated-sub-guid>"]`
- `rg_include`: `["payments-prod-rg", "checkout-prod-rg"]`
- `template_repo_path`: `infra/main.json`
- `resource_types_to_check`: 4 types
- `ruleset_hash8`: `a3f8c1d2` (simulated SHA-256 prefix of rules.yaml)
- `run_id`: `DRIFT-20260512-prod-a3f8c1d2`

### Step 2 — Load ARM template

- Read `infra/main.json` (simulated). Found 8 resources matching `resource_types_to_check`.

### Step 3 — Per-rule drift detection loop

**Rule R001 — NSG security rule drift**

```
generate_query(kql=<R001 KQL with tokens substituted>)
→ execute_query(...)
```

- Found 2 NSGs in live; matched 2 from template.
- `prod-nsg-web`: `securityRules[0].destinationPortRange` differs — template=`443`, live=`8443`.
- `prod-nsg-internal`: IN_SYNC.

**Rule R002 — VM SKU / size drift**

```
generate_query(kql=<R002 KQL with tokens substituted>)
→ execute_query(...)
```

- Found 2 VMs in live; matched 2 from template.
- `prod-vm-api01`: `vmSize` differs — template=`Standard_D4s_v5`, live=`Standard_D2s_v3`.
- `prod-vm-db01`: IN_SYNC.

**Rule R003 — Storage account SKU and network ACL drift**

```
generate_query(kql=<R003 KQL with tokens substituted>)
→ execute_query(...)
```

- Found 2 storage accounts in live; matched 2 from template.
- `prodstorageacct`: `sku.name` differs (template=`Standard_GRS`, live=`Standard_LRS`);
  `networkAcls.defaultAction` differs (template=`Deny`, live=`Allow`).
- `proddiagstg`: IN_SYNC.

**Rule R004 — Key Vault access policy drift**

```
generate_query(kql=<R004 KQL with tokens substituted>)
→ execute_query(...)
```

- `prod-kv-secrets`: No matching live row returned → MISSING_IN_LIVE.

**Rule R005 — Generic resource property drift (catch-all)**

```
generate_query(kql=<R005 KQL with all resource types substituted>)
→ execute_query(...)
```

- All resources already covered by R001–R004 or IN_SYNC. No additional drift found.

### Step 4 — Aggregate

- `resources_total`: 8
- `resources_drifted`: 3
- `resources_missing`: 1
- `resources_clean`: 4

### Step 5 — Render + export

- Rendered `templates/output-report.md` with all placeholders substituted.
- Written to `exports/drift-prod-latest.md`.
